/**
 * Shared UI font stacks. The display/mono faces are bundled woff2 files
 * (public/fonts, loaded via @font-face in index.html); stacks fall back to
 * system fonts while the faces load or when unavailable.
 */
export const UI_FONT_DISPLAY = '"Orbitron", "Arial Black", "Impact", "Helvetica Neue", Arial, sans-serif';
export const UI_FONT_MONO = '"Share Tech Mono", "Courier New", monospace';
