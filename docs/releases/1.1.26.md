# Release 1.1.26 - smooth impacts and finish residual cleanup

Finish the residual quality pass with stable gameplay-frame wiring, smaller private contracts, stricter entry-point analysis, and calmer impact feedback that keeps the playfield readable.

## Highlights

- Reuses stable update-frame and gameplay-behavior delegates across frames.
- Preserves live helper-wing replacement through the cached gameplay behavior.
- Removes camera shake from routine explosions and accepted player impacts while retaining particles, debris, sound, and hit events.
- Consolidates six GameScene bootstrap contract files into one private contract module.
- Inlines forwarding-only parallax context builders and removes dead private surface.
- Adds explicit Knip coverage for `index.html` and repository scripts.
- Adds focused regression coverage for update-frame ordering, cached delegate behavior, and atmosphere target calculations.

## What changed

- Runtime smoothness:
  - `src/scenes/GameScene.ts`
  - `src/scenes/gameScene/gameplayFrameBehavior.ts`
  - `src/scenes/gameScene/updateFrame.ts`
  - `src/systems/CollisionManager.ts`
  - `src/systems/EffectsManager.ts`
  - GameScene now holds stable frame delegates, live helper-wing state remains observable, and routine impact feedback no longer moves the camera.

- Contract and lifecycle cleanup:
  - `src/scenes/gameScene/bootstrapContracts.ts`
  - `src/scenes/gameScene/runGameSceneCreateBootstrap.ts`
  - `src/systems/ParallaxBackground.ts`
  - `src/systems/parallax/premiumBackgroundLayers.ts`
  - `src/systems/parallax/resizeRebuildOrchestration.ts`
  - One-use forwarding modules and unused exports were removed without changing their orchestration behavior.

- Quality coverage:
  - `package.json`
  - `tests/GameSceneUpdateGate.test.ts`
  - `tests/ParallaxBackground.test.ts`
  - `tests/atmosphereProfile.test.ts`
  - `tests/updateFrame.test.ts`
  - Entry-point analysis now includes browser and script roots, while focused tests cover the extracted pure behavior and cached update path.

## Lore

Constraint: Preserve campaign behavior, Phaser scene ownership, and existing combat feedback while reducing hot-path allocation and removing forwarding-only private seams.
Rejected: Configurable routine impact shake | The desired default is a stable viewport, and retaining a dormant option would preserve unnecessary surface.
Rejected: Keep one contract file per bootstrap phase | The files only split type declarations and added navigation overhead without independent ownership.
Confidence: high
Scope-risk: low
Directive: Keep moment-to-moment combat readable and smooth while making private orchestration proportional to the behavior it owns.
Tested: `bun run build`; `bun run typecheck`; `bun run lint`; `bun run test`; `bun run levels:validate`; `bun run knip`; `bun run bundle:check`.
Not-tested: Manual browser/mobile smoke was not run in this terminal session.
