# Release 1.1.10 — Mobile pause button placement polish

## Highlights

- Moves the mobile pause button lower so it clears the gameplay HUD and reads as a distinct touch control.

## What changed

- Mobile pause placement:
  - `src/systems/MobileControls.ts`
  - Increased the touch pause button's top inset so the control now sits below the HUD band with better spacing on mobile gameplay screens.

## Validation

- `bun test`
- `bun run levels:validate`
- `bun run lint`
- `bun run build`
- `bun run knip`
- `bun run bundle:check`
