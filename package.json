{
  "name": "space-explorer",
  "version": "1.1.25",
  "author": "Douwe de Vries",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.3.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "bun test --max-concurrency=1",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "build": "tsc -p tsconfig.json && vite build",
    "bundle:report": "bun scripts/bundle-report.mjs",
    "bundle:check": "bun scripts/bundle-report.mjs --check",
    "preview": "vite preview",
    "lint": "eslint .",
    "knip": "knip",
    "levels:validate": "bun scripts/validateLevels.ts"
  },
  "knip": {
    "ignore": [
      "tests/types/bun-test.d.ts"
    ],
    "project": [
      "src/**/*.ts",
      "tests/**/*.ts",
      "tests/**/*.d.ts",
      "scripts/**/*.ts",
      "scripts/**/*.mjs"
    ]
  },
  "dependencies": {
    "phaser": "^4.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.3.14",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "knip": "^6.6.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.0",
    "vite": "^8.0.10"
  }
}
